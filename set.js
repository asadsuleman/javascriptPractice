// making a net set of items/values
//syntax
const newSet = new Set(["asad", "suleman", "sarazai", "baloch", "asad"]); //there are to values are same it automatically marge it

// newSet.add("ali")
// newSet.delete("asad")


// running for of loop on it

for (i in newSet) {
    console.log('i   ', i)
}

let n1 = newSet;
console.log('n1   ', n1);